{
  "name": "react-native-device-size",
  "version": "0.1.0",
  "description": "classify mobile device size in react-native",
  "devDependencies": {
    "babel-eslint": "^8.2.2",
    "eslint": "^4.4.0",
    "eslint-config-google": "^0.9.1",
    "flow-bin": "^0.66.0",
    "jest": "^22.4.2",
    "jest-react-native": "^18.0.0",
    "react": "16.2.0",
    "react-native": "^0.54.0"
  },
  "main": "./index",
  "scripts": {
    "test": "jest",
    "lint": "./node_modules/.bin/eslint src",
    "flow": "./node_modules/.bin/flow",
    "prepublish": "yarn run lint && yarn run flow && yarn run test"
  },
  "jest": {
    "preset": "react-native"
  },
  "dependencies": {},
  "license": "MIT"
}
